name: AdEx Classification

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run AdEx Classification
        run: python adex_qml_pipeline.py --task classification

      - name: Upload CSV outputs (AdEx classification)
        uses: actions/upload-artifact@v4
        with:
          name: adex-classification-csv
          path: |
            eval_classification*.csv
          if-no-files-found: error
