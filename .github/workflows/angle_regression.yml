name: Angle Regression

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run Angle Regression
        run: python qml_angle_pipeline.py --task regression

      - name: Upload CSV outputs (Angle regression)
        uses: actions/upload-artifact@v4
        with:
          name: angle-regression-csv
          path: |
            eval_regression_angle*.csv
          if-no-files-found: error
